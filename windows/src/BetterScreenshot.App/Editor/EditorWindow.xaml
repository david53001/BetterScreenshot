<Window x:Class="BetterScreenshot.App.Editor.EditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BetterScreenshot Editor" Width="960" Height="720" MinWidth="560" MinHeight="460"
        WindowStartupLocation="CenterScreen" Background="{StaticResource Theme.WindowBrush}">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="{StaticResource Theme.ChromeBrush}" Padding="8,6">
            <StackPanel>
                <StackPanel x:Name="Toolbar" Orientation="Horizontal" HorizontalAlignment="Center"/>
                <StackPanel x:Name="Inspector" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,6,0,0"/>
            </StackPanel>
        </Border>
        <Border DockPanel.Dock="Bottom" Background="{StaticResource Theme.ChromeBrush}" Padding="12,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Done" Click="Done_Click" Padding="14,6" Margin="6,0,0,0"/>
                <Button Content="Stack" Click="Stack_Click" Padding="14,6" Margin="6,0,0,0"/>
                <Button Content="Save" Click="Save_Click" Padding="14,6" Margin="6,0,0,0"/>
                <Button Content="Copy" Click="Copy_Click" Padding="14,6" Margin="6,0,0,0"
                        Style="{StaticResource Theme.AccentButton}"/>
            </StackPanel>
        </Border>
        <!-- Pasteboard backdrop: the window auto-sizes to the image aspect (FitToImage) so this is
             mostly hidden; any residual frame reads as an intentional dark mat, not an empty gap. -->
        <Grid x:Name="CanvasArea" Background="{StaticResource Theme.ChromeBrush}">
            <Viewbox Stretch="Uniform" Margin="16">
                <Grid x:Name="Stage">
                    <Image x:Name="CanvasImage" Stretch="Fill"/>
                    <Canvas x:Name="InteractionLayer" Background="Transparent"/>
                </Grid>
            </Viewbox>
        </Grid>
    </DockPanel>
</Window>
