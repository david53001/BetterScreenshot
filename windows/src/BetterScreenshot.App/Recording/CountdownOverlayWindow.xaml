<Window x:Class="BetterScreenshot.App.Recording.CountdownOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ShowActivated="False"
        Width="200" Height="200" WindowStartupLocation="Manual">
    <Border CornerRadius="24" Background="#E61C1C1C">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>
        <TextBlock x:Name="Digit" Text="0" Foreground="White" FontFamily="Consolas"
                   FontSize="120" FontWeight="SemiBold"
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Border>
</Window>
